/**
 * Application configuration
 * API keys and settings are stored here directly.
 */

export const config = {
  gemini: {
    apiKey: "your_gemini_key",
    model: "gemini-3.1-flash-lite-preview",
  },
} as const;
